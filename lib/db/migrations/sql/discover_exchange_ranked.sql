-- Ranking de coleccionistas por oportunidad de intercambio (misma ciudad).
-- Migración oficial: migrations/0008_exchange_wants_and_discover_rank.sql
--
-- Objeto relacionado:
-- - Tabla exchange_wants (prioridades marcadas desde el álbum).
-- - Función discover_collectors_exchange_ranked(p_user_id, p_limit).

-- Ver el archivo de migración 0008 para el SQL completo (CREATE TABLE + CREATE FUNCTION).
